{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --since",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "name": "veterinary",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
